import * as React from "react";
import type { TransitionProps } from "@/types/ui";

export const Transition: React.FC<TransitionProps> = ({ children }) => {
  // This is a placeholder for a real implementation (e.g., using framer-motion).
  return <>{children}</>;
};
Transition.displayName = "Transition";
